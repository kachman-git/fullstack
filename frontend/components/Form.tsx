import React from "react";
import { FormField } from "./ui/form";

interface FormProps {
  name: string;
  placeholder: string;
  discription: string;
}

const Form = ({ name, placeholder, discription }: FormProps) => {};

export default Form;
